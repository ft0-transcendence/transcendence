services:
  app:
    image: node:22.17.0
    container_name: transcendence
    restart: unless-stopped
    working_dir: /app
    volumes:
      - .:/app
    env_file:
      - ./.env
    command: npm run start:backend

